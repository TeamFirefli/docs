---
title: "Create Notice"
openapi: "POST /api/public/v1/workspace/{workspaceId}/notices"
description: "Create a new notice/timeoff request for a user."
tags: ["notices"]
icon: "plus"
---

## Summary

Create a new timeoff notice for a workspace member.

By default, notices are **auto-approved**. Pass `reviewed: false` in the request body to leave the notice in a pending state, requiring a manager to manually approve or reject it.